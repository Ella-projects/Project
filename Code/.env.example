# Copy to .env and fill in values before running

# Database
DATABASE_URL=postgresql+asyncpg://aui:aui@localhost:5432/adaptiveui

# Rough Set thresholds (optional overrides)
LOWER_APPROX_THRESHOLD=0.75
BOUNDARY_REGION_THRESHOLD=0.45

# IAT
IAT_TRIAL_COUNT=80
IAT_PRACTICE_TRIAL_COUNT=20
